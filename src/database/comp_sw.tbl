CREATE TABLE comp_sw (
  sw_id          INT (10) AUTO_INCREMENT PRIMARY KEY,
  date           DATETIME NOT NULL,
  dmodswid       CHAR (24),
  distort        CHAR (28),
  sw_version     CHAR (24),
  labviewid      VARCHAR (20),
  sw_revision    VARCHAR (20),
  UNIQUE (date)
)