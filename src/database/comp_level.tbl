-- there will be several entries in this table

create table comp_level (
  level_id       tinyint (2) auto_increment primary key,
  level          char (5) not null,
  description    varchar (512)
)
